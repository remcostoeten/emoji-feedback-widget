export type FeedbackData = {
  opinion: string;
  feedback: string;
};

export type OpinionEmoji = {
  text: string;
  emoji: string;
};

export type FeedbackData = {
  opinion: string;
  feedback: string;
};
